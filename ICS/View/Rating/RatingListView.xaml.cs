using ICS.ViewModel.Activity;
using ICS.ViewModel.Rating;

namespace ICS.View.Rating;

public partial class RatingListView
{
    public RatingListView(RatingListViewModel viewModel) : base(viewModel)
    {
        InitializeComponent();
    }
}